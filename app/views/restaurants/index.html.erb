<% if @restaurants.any? %>
	<% @restaurants.each do | item | %>
		<%= item.name %>
	<%end%>

<% else %>

	<h3>There is no restaurants</h3>

<% end %>

<%= link_to 'Add restaurant', new_restaurant_path %>